/**
 * DAG: Design a linear-time algorithm to determine whether a DAG has a vertex
 * that is reachable from every other vertex, and if so, find one.
 * Digraph: Design a linear-time algorithm to determine whether a digraph has
 * a vertex that is reachable from every other vertex, and if so, find one.
 */
public class ReachableVertex {
	
}